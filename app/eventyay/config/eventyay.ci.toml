# CI configuration
# Extends testing behavior with explicit service configuration

# --- Testing behavior ---
email_backend = "django.core.mail.backends.locmem.EmailBackend"
celery_always_eager = true

# --- General ---
secret_key = "ci-test-secret-key"
debug = true

# --- Database ---
[database]
engine = "django.db.backends.postgresql"
name = "eventyay-db"
user = "eventyay"
password = "eventyay"
host = "localhost"
port = 5432

# --- Redis ---
[redis]
url = "redis://localhost:6379/0"

# --- Environment ---
[environment]
name = "ci"
